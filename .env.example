PORT=8080
DB_USERNAME=postgres
DB_PASSWORD=
DB_DATABASE=«db_name»
DB_HOST=localhost
JWT_SECRET=<secret generated with require("crypto").randomBytes(32).toString("hex");>
JWT_EXPIRES_IN=<time till token expires in seconds>
